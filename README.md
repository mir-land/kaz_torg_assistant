# 🛒 SmartRetail Kiosk — ИИ Ассистент для магазина

**Голосовой ИИ-помощник** для торговых точек с поддержкой казахского и русского языков.

> **Статус:** Бета-версия. Доступна для тестирования.

---

## 🎯 Что умеет

- 🎙️ **Слышит клиента** — распознаёт речь на казахском и русском (Soniox STT)
- 🧠 **Отвечает умно** — генерирует ответы через LLM (Qwen/GPT)
- 🔊 **Говорит вслух** — синтез речи (Azure TTS)
- 🔄 **AEC** — подавление эха, можно перебить агента во время речи
- 👥 **Режим продавца** — продавец может подключиться к диалогу
- 📱 **Два режима UI** — Touch (сенсорный киоск) и Glass (прозрачный)

---

## 🚀 Быстрый старт (готовый .exe)

1. Скачать последний релиз → **[Releases](../../releases)**
2. Распаковать в любую папку
3. Запустить `SmartRetailKiosk.exe`
4. Открыть ⚙️ Настройки → выбрать микрофон → Сохранить

> **Требования:** Windows 10/11, микрофон, интернет

---

## 🛠️ Запуск из исходников

### 1. Установить зависимости

```bash
pip install PySide6 soniox openai pyaudiowpatch azure-cognitiveservices-speech
```

> ⚠️ `pyaudiowpatch` (не `pyaudio`) — нужен для AEC (подавление эха)

### 2. Настроить ключи

```bash
cp config.example.py config.py
# Открыть config.py и вставить API ключи
```

Нужны ключи:
| Сервис | Где получить |
|--------|-------------|
| **Soniox** (STT) | [soniox.com](https://soniox.com) |
| **Azure Speech** (TTS) | [portal.azure.com](https://portal.azure.com) |
| **Qwen/OpenAI** (LLM) | [dashscope.console.aliyun.com](https://dashscope.console.aliyun.com) |

### 3. Запустить

```bash
python main.py
```

---

## 🏗️ Сборка .exe

```bash
pip install pyinstaller
build_quick.bat
```

Результат: `dist2/SmartRetailKiosk/SmartRetailKiosk.exe`

---

## 📁 Структура проекта

```
src/
├── core/
│   ├── orchestrator.py      # Основная логика
│   ├── audio_capture.py     # Захват микрофона + AEC
│   └── settings_manager.py  # Настройки
├── adapters/
│   ├── soniox_adapter.py    # STT
│   ├── azure_tts_adapter.py # TTS
│   └── llm_adapter.py       # LLM
├── ui/
│   ├── client_window.py     # Окно клиента (киоск)
│   └── seller_window.py     # Окно продавца
└── prompts/
    └── system_prompt.txt    # Промпт ассистента
```

---

## 📄 Лицензия

Проприетарное ПО. Использование только по лицензии.  
По вопросам: [@mir-land](https://github.com/mir-land)
